# mac-tauri

macOS Tauri application shell for SelfDeploy.

The shell owns macOS bundle config, icons and startup wiring only. Shared backend behavior lives in `../../packages/tauri-core`, and shared UI lives in `../shared-renderer`.
